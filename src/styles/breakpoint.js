const size = {
  sm: '768px',
  lg: '1200px',
  xl: '1800px'
}

const device = {
  sm: `(min-width: ${size.sm})`,
  lg: `(min-width: ${size.lg})`,
  xl: `(min-width: ${size.xl})`,
}

export default {size, device}